[package]
name = "@overlay/overlay"
version = "0.1.0"
edition = "2026"
description = "EP overlay key-graph over data-storage transactions with function key sets"
license = "MIT OR Apache-2.0"
repository = "https://github.com/murphsicles/overlay"
keywords = ["overlay", "bsv", "cryptography", "security"]
categories = ["cryptography"]
